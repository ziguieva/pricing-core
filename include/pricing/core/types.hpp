#pragma once

#include <cstdint>

namespace pricing {

/**
 * Strong types to avoid unit and semantic errors.
 */
using Real = double;
using Time = double;
using Size = std::uint64_t;

} // namespace pricing
